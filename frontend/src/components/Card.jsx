export default function Card({issue, issueDesc}) {

    return (
        <div>
                      <div>
                            <div>{issue}</div>
                            <div>{issueDesc}</div>
                        </div>
        </div>
    )
}